<%= form_for [@environment, @variable], html: {class: 'form'} do |f| %>
  <% if @variable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@variable.errors.count, "error") %> prohibited this variable from being saved:</h2>

      <ul>
      <% @variable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <%= f.label :key %>
    <%= f.text_field :key %>
  </div>
  <div class="row">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit('Create', class: 'button') %>
  </div>
<% end %>
